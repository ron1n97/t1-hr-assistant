
  # User Profile Interface

  This is a code bundle for User Profile Interface. The original project is available at https://www.figma.com/design/TVpNYH36lfzracCpXCV44F/User-Profile-Interface.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  